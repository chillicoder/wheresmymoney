.block
  .secondary-navigation
    %ul.wat-cf
      %li.first=link_to pat(:list), url(:money, :index)
      %li.active=link_to pat(:new), url(:money, :new)
  .content
    %h2.title
      =pat(:new)
      =mt(:money)
    .inner
      -form_for :money, url(:money, :create), :class => :form do |f|
        =partial "money/form", :locals => { :f => f }

-content_for :sidebar, partial("base/sidebar")